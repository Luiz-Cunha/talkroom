<h1>Welcome, <%= @user.email %>!</h1>

<h2>Your Profile Information</h2>
<p>Email: <%= @user.email %></p>
<p>Account Created At: <%= @user.created_at.strftime("%B %d, %Y") %></p>

<h2>Your Previous Appointments</h2>
<% if @appointments.any? %>
  <ul>
    <% @appointments.each do |appointment| %>
      <li>
        Appointment on <%= appointment.schedule_time.strftime("%B %d, %Y at %I:%M %p") %>
        <br>
        Symptom: <%= appointment.symptom %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>You have no previous appointments.</p>
<% end %>

